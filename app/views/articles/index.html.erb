<h1>Articles </h1>

<ul>
	<% @articles.each do |article| %>
	<li>
		<a href="<%= article_path(article)%>">
			<%= article.title %>
		</a>	
	</li>

	<li>
		<a href="<%= article_url(article)%>">
			<%= article.title %>
		</a>	
	</li>
	<% end %>
</ul>